/*
HELP! Jason can't find his textbook! It is two days before the test date, and Jason's 
textbooks are all out of order! Help him sort a list (ArrayList in java) full of 
textbooks by subject, so he can study before the test.
The sorting should NOT be case sensitive
*/

let sorter = textbooks =>
  // if the lowercase version of one book is greater than the lowercase version of the other, put the first one before the second
  textbooks.sort((a, b) => a.toLowerCase() > b.toLowerCase() ? 1 : -1)

let books = ['Geometry', 'history', 'Algebra', 'english']

sorter(books)
